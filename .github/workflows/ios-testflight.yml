name: iOS Build & TestFlight

on:
  push:
    branches: [ main ]

jobs:
  ios:
    runs-on: macos-latest
    env:
      NODE_ENV: development

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      # ─── Desactiva Husky para TODO npm install/ci ──────
      - name: 🚫 Disable Husky hooks in CI
        run: echo "HUSKY=0" >> $GITHUB_ENV

      # ─── Ruby + Bundler (cache opcional) ────────────────
      - name: 💎 Set up Ruby & Bundler
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1.7
          bundler-cache: true

      # ─── Node.js + npm cache ────────────────────────────
      - name: 🔧 Set up Node.js & cache npm
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      # ─── Instala deps JS ────────────────────────────────
      - name: 📦 Install JS dependencies
        run: npm ci

      # ─── Nuevo paso: descarga el Podspec oficial ────────
      - name: 📥 Fetch react-native-maps-generated.podspec
        run: |
          curl -L \
            https://raw.githubusercontent.com/react-native-maps/react-native-maps/v1.23.7/react-native-maps-generated.podspec \
            -o node_modules/react-native-maps/react-native-maps-generated.podspec


      # ─── Cache CocoaPods & pod install ──────────────────
      - name: 🧩 Cache CocoaPods
        uses: actions/cache@v3
        with:
          path: |
            ios/Pods
            ~/.cocoapods
          key: ${{ runner.os }}-cocoapods-${{ hashFiles('ios/Podfile.lock') }}

      - name: 🛠️ Install iOS pods
        working-directory: ios
        run: pod install --repo-update --verbose

      # ─── Decodifica certificados y perfiles ─────────────
      - name: 🔑 Decode distribution certificate
        env:
          APPLE_CERT:    ${{ secrets.APPLE_CERTIFICATE }}
          CERT_PASSWORD: ${{ secrets.CERT_PASSWORD }}
        run: |
          echo "$APPLE_CERT" | base64 --decode > ios/ios_distribution.p12
          security create-keychain -p "" build.keychain
          security import ios/ios_distribution.p12 \
            -k ~/Library/Keychains/build.keychain \
            -P "$CERT_PASSWORD" \
            -T /usr/bin/codesign
          security list-keychains -s ~/Library/Keychains/build.keychain

      - name: 📄 Decode provisioning profile
        env:
          PROFILE: ${{ secrets.PROVISIONING_PROFILE }}
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$PROFILE" | base64 --decode > \
            ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision

      - name: 🔐 Decode App Store Connect API Key
        env:
          ASC_KEY: ${{ secrets.APPSTORE_API_KEY }}
          KEY_ID:  ${{ secrets.APPSTORE_KEY_ID }}
        run: |
          echo "$ASC_KEY" | base64 --decode > ios/AuthKey_${KEY_ID}.p8

      # ─── Ejecuta Fastlane ───────────────────────────────
      - name: 🚀 Run Fastlane beta
        working-directory: ios
        run: bundle exec fastlane beta
