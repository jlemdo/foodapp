import colors from './colors';
import fonts from './fonts';
import spacing from './spacing';

const theme = {
    colors,
    fonts,
    spacing,
};

const lightTheme = {
    colors: {
        background: '#ffffff',
        text: '#333333',
    },
};

const darkTheme = {
    colors: {
        background: '#333333',
        text: '#ffffff',
    },
};

export default theme;
