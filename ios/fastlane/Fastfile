platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    build_app(
      workspace:      "MyNewApp.xcworkspace",
      scheme:         "MyNewApp",
      clean:          true,
      export_method:  "app-store",

      # Deja que Xcode elija automáticamente el provisioning profile
      xcargs: [
        "DEVELOPMENT_TEAM=#{ENV['APPLE_TEAM_ID']}",
        "CODE_SIGN_STYLE=Automatic",
        "PRODUCT_BUNDLE_IDENTIFIER=#{ENV['APP_IDENTIFIER']}"
      ].join(" "),

      # Puedes quitar export_xcargs si ya no necesitas allowProvisioningUpdates
      export_team_id: ENV["APPLE_TEAM_ID"],
      export_options: {
        provisioningProfiles: {
          ENV["APP_IDENTIFIER"] => "OcrProductos_Release_Profile"
        }
      }
    )
    upload_to_testflight
  end
end
