default_platform(:ios)

platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    build_app(
      export_method: "app-store",
      scheme: "MyNewApp" # Asegúrate que coincida con el nombre del esquema de tu app
    )
    upload_to_testflight
  end
end
